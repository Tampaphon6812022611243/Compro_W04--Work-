#include <stdio.h>  

int main(){  // ฟังก์ชันหลักของโปรแกรม เป็นจุดเริ่มต้นการทำงาน
    printf("Hello,\n");  // แสดงข้อความ "Hello," และขึ้นบรรทัดใหม่ (\n คือ newline)
    printf("C Tokens!"); // แสดงข้อความ "C Tokens!" ต่อจากบรรทัดที่แล้ว
    return 0;  // ส่งค่ากลับ 0 แสดงว่าโปรแกรมจบการทำงานอย่างถูกต้อง
}