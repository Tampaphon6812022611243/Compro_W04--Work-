#include <stdio.h>  
int main(){  // ฟังก์ชันหลักของโปรแกรม เป็นจุดเริ่มต้นการทำงาน
    int a = 10;  // ประกาศตัวแปรชนิดจำนวนเต็มชื่อ a และกำหนดค่าเริ่มต้นเป็น 10
    printf("a = %d", a);  // แสดงข้อความ "a = " ตามด้วยค่าของตัวแปร a โดย %d ใช้แทนค่าจำนวนเต็ม
    return 0 ;  // ส่งค่ากลับ 0 เพื่อบอกว่าโปรแกรมจบการทำงานอย่างถูกต้อง
}